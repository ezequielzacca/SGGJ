{% extends 'GremioAbmBundle::layout.html.twig' %}
{% block body %}
<div class="mws-panel grid_4">
                    
    <div class="mws-panel-header">
                    	<span>Datos de la Localidad</span>
                    </div>
    <div class="mws-panel-body">
        <div class="mws-panel-toolbar bottom clearfix">
                        	<ul>
                                <li><a href="{{ path('administracion_entidades_localidad') }}" class="mws-ic-16 ic-arrow-left">Volver a la Lista</a></li>
                            	<li><a href="{{ path('administracion_entidades_localidad_edit', { 'id': entity.id }) }}" class="mws-ic-16 ic-pencil">Editar</a></li>                            	
                            </ul>
                        </div>
        <div class="mws-panel-content">

<table class="mws-table">
    <tbody>
        {#<tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>#}
        <tr>
            <th>Nombre</th>
            <td>{{ entity.Nombre }}</td>
        </tr>
        <tr>
            <th>Codigo Postal</th>
            <td>{{ entity.CodigoPostal }}</td>
        </tr>
    </tbody>
</table>

            </div>
            </div>
    </div>
                          
                </div>
    
        
   
{% endblock %}