{% extends "GremioSecurityBundle::layout.html.twig" %}
{% block body %}
<h1>User creation</h1>

<form action="{{ path('Security_User_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('Security_User') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}